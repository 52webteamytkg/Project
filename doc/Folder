フィルダ構成　

├/doc    ->  開発ドキュメント等を保存する場所
│
├/resrc  ->　画像ファイル等を保存する場所
│
├/www    ->　Webページ（本番用）
│
└/www.dev->　Webページ（開発用）

git手順書

① github上からクローン作成。
　※SourcetreeのローカルリポジトリでURLからクローンを作成。

② master brunchから　sourceTreeで編集用のbrunchを作成し。
　　※New brunch 作成時　名前は　feat/タスク名にすること

③各々エラーが出ないことを確認、コミット→プッシュ

④プルリクエストを確認して、リモート上でエラーがないことを確認。

⑤他のメンバーが確認後マージ。
